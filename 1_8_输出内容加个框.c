/*************************************************************************
	示例运行步骤：
	1. 用户输入 "Hello, World!"。
	2. scanf 函数将输入的字符串 "Hello, World!" 读取并存储在数组 s 中。
	3. sprintf 函数将字符串 s 格式化为 "| Hello, World! |" 并存储在数组 t 中，同时返回写入字符串的字符数（包括空字符），存储在 n 中。
	4. 第一个 for 循环输出 n 个连字符 -。
	5. printf 函数输出格式化后的字符串 "| Hello, World! |"。
	6. 第二个 for 循环再次输出 n 个连字符 -。
 ************************************************************************/

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	char s[100], t[100]; // 数组 s 用于存储输入的字符串，数组 t 用于存储格式化后的字符串
	// scanf 函数用于从标准输入读取字符串并存储在数组 s 中。
	scanf("%[^\n]", s); // 格式说明符 %[^\n] 表示读取所有字符直到遇到换行符（\n）。^ 表示取反，即读取除换行符以外的所有字符
	int n = 0; //声明并初始化一个整型变量 n，初始值为0。n 将用于存储 sprintf 函数返回的字符数。
	// sprintf 函数用于将格式化的数据写入字符串 t 中。sprintf 函数返回写入字符串的字符数（包括空字符），这个值被存储在 n 中。
	n = sprintf(t, "| %s |", s); //格式字符串 "| %s |" 指定 sprintf 将字符串 s 格式化为 | s | 的形式，并存储在 t 中。
	// for 循环用于输出 n 个连字符 -。循环从 i = 0 开始，每次循环 i 增加1，直到 i < n 条件为假时结束
	for (int i = 0; i < n; i++) printf("-");
	printf("\n");
	printf("| %s |\n", s);
	for (int i = 0; i < n; i++) printf("-");
	printf("\n");
	return 0
}

/*************************************************************************
	计算机底层原理：
	内存分配：字符数组 s 和 t 被分配在栈内存上。每个数组占用100个字节的连续内存空间。
	指针和地址运算：scanf 和 sprintf 函数需要使用指针来存储和读取数据，因此我们传递数组名（即指向数组第一个元素的指针）给这些函数。
	格式化字符串：格式字符串在 sprintf 中起到了模板的作用，%s 被输入的字符串替换，| 和空格作为固定字符插入到结果中。

	扩展知识:
	缓冲区溢出：使用 scanf 时要小心输入长度超过缓冲区大小的情况，这可能导致缓冲区溢出。可以使用 fgets 代替 scanf 来读取字符串，以确保不会超出缓冲区大小。
	字符串处理函数：除了 sprintf，C标准库还提供了许多字符串处理函数，如 strcpy、strcat、strlen 等，用于不同的字符串操作。
	循环和条件控制：for 循环和 if 语句是控制程序流程的重要工具，理解它们的使用是编写复杂程序的基础
 ************************************************************************/
